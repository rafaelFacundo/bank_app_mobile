import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  questionText: (fontSize) => ({
    width: "100%",
    fontFamily: "Poppins",
    fontSize: fontSize,
  }),
});

export default styles;
