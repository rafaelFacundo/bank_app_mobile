import { StyleSheet } from "react-native";

const styles = (fontSize: any) =>
  StyleSheet.create({
    questionText: {
      width: "100%",
      fontFamily: "Poppins",
      fontSize: fontSize,
    },
  });

export default styles;
