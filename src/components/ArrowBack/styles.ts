import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  arrowBackButton: {
    position: "absolute",
    top: 35,
    left: 10,
    zIndex: 5,
  },
  arrowBackImage: {
    width: 80,
    height: 80,
  },
});

export default styles;
